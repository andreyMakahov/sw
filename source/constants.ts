export const DEFAULT_PREFIX: string = 'sw';

export const DEFAULT_SUFFIX: string = 'v1';

export const DEFAULT_PRECACHE_NAME: string = 'precache';

export const DEFAULT_RUNTIME_NAME: string = 'runtime';

export const DEFAULT_DEBUG_VALUE: boolean = true;

export enum Modes {
    Development = 'DEVELOPMENT',
    Production = 'PRODUCTION',
}
